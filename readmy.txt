primeiro instalei o pgadmin, depois instalei (nexte em tudo),;
abri o prisma, depois fiz create database, fiz login com github, depois dei nome;
no prisma abri o Quikstart e copiei o codigo para o .env;

executar isto:
npm install prisma @prisma/client
npx prisma init

depois vai criara isto:
/prisma
   schema.prisma

Depois vou ao prisma e abro o conected e copio o primeiro link;
depois coloco isto no pgadmin:


No separador "Connection":
Host name/address: db.prisma.io
(É apenas o que está entre o @ e os dois pontos do porto).

Port: 5432

Maintenance database: postgres
(É o nome que aparece depois da barra / final).

Username: 233cf0dd3106b3a308b8172349a1b39b6cee75e37861770faee6b297405109cd
(Sim, este código gigante entre o // e os : é o teu utilizador).

Password: sk_-ttHlgPqsbymczT7BppvR
(O código que está entre os : e o @).



Depis desconectei a base de dados do servidor fui a propriadades e alterarei o campo para (require);
Depois criei a base de dados no schema.prisma e dpois usei o comando (npx prisma db push);
Logo de seguida alterei o server.js;
Depois este comado (npm install @prisma/extension-accelerate), e depois este (npx prisma generate);

Neste momento ja esta funcional com localhost, por isso temos de atulizar algumas coisar para colocar a correr no versel;

Versel, tres pontos e depois Settings depois em Environment Variables vamos adicionar o DATABASE_URL e a SERVER_PORT defenidaa no .env;

Depois no packege.json temos de atualizar os scripts:

"scripts": {
  "postinstall": "prisma generate",
  "build": "prisma generate",
  "start": "node server.js",
  "dev": "nodemon server.js"
}




No final fazemos cmmit e push e podemos abri no versel a api;